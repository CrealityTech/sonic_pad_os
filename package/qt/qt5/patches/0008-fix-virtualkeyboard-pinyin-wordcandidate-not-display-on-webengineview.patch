--- a/qtvirtualkeyboard/src/virtualkeyboard/qvirtualkeyboardinputcontext_p.cpp
+++ b/qtvirtualkeyboard/src/virtualkeyboard/qvirtualkeyboardinputcontext_p.cpp
@@ -392,7 +392,7 @@ void QVirtualKeyboardInputContextPrivate
     // update input engine
     if ((newSurroundingText || newCursorPosition) &&
             !testState(State::InputMethodEvent)) {
-        commit();
+        //commit();
     }
     if (newInputMethodHints) {
         reset();
